# Qwen inference (Docker)

Two services: **original Qwen (PyTorch)** and **ONNX Qwen**. Build from project root with `./scripts/build_images.sh cpu` or `./scripts/build_images.sh cuda`.

**All commands:** [COMMANDS.md](COMMANDS.md)
